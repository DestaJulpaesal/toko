import { useState } from 'react';
import { apiFetch } from '../services/api';
export default function AccountTransferModal({ accounts = [], onDone }) {
  const [form, setForm] = useState({ fromAccountId: '', toAccountId: '', amount: '', note: '' });
  const submit = async (event) => { event.preventDefault(); const response = await apiFetch('/finance/accounts/transfer', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ ...form, amount: Number(form.amount) }) }); if (response.ok) { setForm({ fromAccountId: '', toAccountId: '', amount: '', note: '' }); onDone?.(); } };
  return <form className="finance-action-form transfer-form" onSubmit={submit}><div className="finance-action-form-heading"><span className="finance-action-icon">↗</span><div><strong>Pindahkan saldo</strong><small>Transfer antar akun atau dompet</small></div></div><div className="finance-action-fields"><label>Dari akun<select value={form.fromAccountId} onChange={(event) => setForm({ ...form, fromAccountId: event.target.value })} required><option value="">Pilih akun asal</option>{accounts.map((account) => <option key={account.id} value={account.id}>{account.name}</option>)}</select></label><label>Ke akun<select value={form.toAccountId} onChange={(event) => setForm({ ...form, toAccountId: event.target.value })} required><option value="">Pilih akun tujuan</option>{accounts.map((account) => <option key={account.id} value={account.id}>{account.name}</option>)}</select></label><label>Nominal<input type="number" min="1" placeholder="Rp 0" value={form.amount} onChange={(event) => setForm({ ...form, amount: event.target.value })} required /></label><label>Catatan<input placeholder="Opsional" value={form.note} onChange={(event) => setForm({ ...form, note: event.target.value })} /></label></div><button className="btn btn-primary">Transfer saldo <span>→</span></button></form>;
}
